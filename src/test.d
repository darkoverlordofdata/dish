import exp from Math;
import log from Math;
import myFunc from myLib;
/*
 *
 * comment
 */
int xInt;
float fFloat;
double zDouble;

export double logSum(double x, int y) {
    int z;
    z = 42 + 13;
    return z;
}

int geometricMean() {
    int x;
    x = 20;
    logSum(x, 10);
}